<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
<LinearLayout
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    android:id="@+id/indexHeader"
    android:layout_margin="10dp"
    >
    <ImageButton
        android:id="@+id/menu"
        android:background="@drawable/menu"
        android:layout_width="24dp"
        android:layout_height="24dp">
    </ImageButton>
    <TextView
        android:id="@+id/headOfIndex"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="今日更新"
        android:layout_weight="1"
        android:gravity="center"
        >
    </TextView>
    <ImageButton
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@drawable/more"></ImageButton>
</LinearLayout>
<androidx.recyclerview.widget.RecyclerView
    android:layout_margin="30dp"
    android:id="@+id/indexCycle"
    app:layout_constraintTop_toTopOf="@id/indexHeader"
    app:layout_constraintStart_toStartOf="@id/indexHeader"
    android:layout_height="wrap_content" android:layout_width="fill_parent"
    ></androidx.recyclerview.widget.RecyclerView>
</androidx.constraintlayout.widget.ConstraintLayout>